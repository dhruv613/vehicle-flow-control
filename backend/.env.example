# Change this only if your Expo Go device needs a different CORS policy.
GARAGE_CORS_ORIGINS=*
# Store demo data outside the repository if desired.
# GARAGE_DATA_FILE=C:\path\to\garage.json
# Set false for a fresh in-memory data set each run.
# GARAGE_PERSIST_DATA=true
